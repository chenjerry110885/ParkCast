/**
 * Placeholder shell. The map, the ranked list and the time slider land in the
 * later tasks of this plan; this exists so the scaffold runs and Task 4 has a
 * root to mount into.
 */
export default function App() {
  return (
    <main>
      <h1>ParkCast</h1>
      <p>停車先知 — probability a Taipei lot has a space when you arrive.</p>
    </main>
  )
}
