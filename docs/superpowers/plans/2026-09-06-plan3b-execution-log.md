# SDD ledger — plan: tasks/todo.md (Plan 3b — Web App core)
Task 1: complete (commit a3e6fa7) — scaffold + grid parser, 9 tests. Verified against the REAL artifact: parsed header matches Python exactly, 26,112 cells, 0 UNKNOWN, mean 87.4%.
  create-vite 9.2.0 no longer sets strict and the root tsconfig is files:[], so a bare tsc --noEmit checks nothing. Implementer added strict + noUncheckedIndexedAccess to both projects and a typecheck script.
Task 1b: complete (commit 38dada9) — loadArtifacts had ZERO coverage; the roster pairing is what stops the app showing lot A's forecast under lot B's name. 6 tests added, implementation was already correct. Mutations verified: pairing on generatedAt fails 3 tests incl. the caching one; returning a mismatched pair instead of throwing fails 1. 15 tests.
  PARKED: fetchLots does not cross-check n_lots against the grid's nLots, only rosterId. Belt-and-braces — rosterId hashes the ordered lot-id list, so a length change already changes the hash.
Task 2: complete (commit 716d81c) — bilingual layer, 20 tests. Districts use Taipei's official signage romanisation (Zhongzheng, Da'an, Xinyi, Neihu...). Lot names deliberately untranslated in both languages.
  FOR USER REVIEW (copy, not correctness): the 8 operator-type English glosses are the implementer's judgment call — no canonical English exists for these categories the way it does for districts.
Task 3: complete (commit b85ab03) — geo + expected-cost ranker, 29 tests. Constants exported and documented at the top of rank.ts: TIME_VALUE 5 NT$/min, EXPECTED_HOURS 2, CIRCLING_PENALTY_MIN 12, MEDIAN_PRICE_FALLBACK 77.5 (re-measured from the shipped lots.json: 1,040 parseable of 1,088; exact 852 / range 188 / entry 21 / unknown 27). Mutations verified: scoring unpriced lots at zero fails the no-price test; dropping unknown-P lots fails the ranked-last test; each broke exactly its own guard. Unknown P gives cost:null and a second sort tier, NOT p=0 — a claim we do not have. entry fares charged once (not x EXPECTED_HOURS) and reported as perEntry with hourly:null, so the UI never invents a per-hour rate no sign displays.
  DEVIATION: brief's tests needed out[0]!.id under noUncheckedIndexedAccess (verified: TS2532 without it). Type-only, erases, no runtime change.
  FOR TASK 4: Ranked.perEntry is non-null for 21 lots that charge a flat fee; a UI reading `hourly` alone renders them as unpriced. Needs a "per entry" i18n string — Strings has perHour but no equivalent, and extending Strings was Task 2's surface.
  FOR PLAN 4: CIRCLING_PENALTY_MIN is the number that turns a forecast into a decision and it is currently my estimate. The spec's decision-level metric ("how often does the top recommendation have a space?") can sweep it against held-out data and make it measured instead.
Task 3: complete (commit b85ab03) — geo + expected-cost ranker, 29 tests. Mutations verified: scoring unpriced lots at zero, and dropping unknown-P lots, each fail their own guard.
  entry pricing charged ONCE not per hour; reported as perEntry with hourly:null so the UI never invents a per-hour rate no sign displays.
  CONTROLLER-VERIFIED on real data: 47 candidates within 800m of Taipei City Hall, top results are real Xinyi car parks (Grand Hyatt NT$80, Far Eastern NT$180, bus terminal NT$100-120) — plausible prices for the district. All at 100% P, so walk+price are the real differentiators there: the saturation caveat showing up in practice.
  CARRIED TO TASK 4: must branch on perEntry and add a 'per entry' string, or 21 lots wrongly render as unpriced.
Task 4: complete (commit 7ca031d) — the screen, 41 tests (12 new). App.tsx + LotList/LotRow + format.ts + a mobile-first index.css replacing the Vite scaffold. i18n gained perEntry and 8 screen keys in both languages. main.tsx needed no change (already mounts App); format.ts is an extra file because exporting formatters from a component file trips oxlint react(only-export-components).
  Mutations verified: branching on `hourly` alone fails the per-entry AND the range test (21 lots wrongly unpriced, range collapses to the ranker's NT$30 midpoint); `(p ?? 0) * 100` fails the no-data test ('0%' where 'No data' belongs).
  VERIFIED ON REAL ARTIFACTS at Taipei City Hall, 360 px, +15 min: top 5 are 松壽廣場 100% / 4 min / NT$10-60, 府前廣場 26% / 3 min / NT$10-30, 三張里 100% / 11 min / NT$30, 松山高中 99% / 10 min / NT$10-60, 興雅國中 100% / 13 min / NT$30. Every field hand-checked against lots.json + grid.bin.
  EXPECTATION GAP EXPLAINED (not a bug): the controller expected the NT$80-200 private lots (君悅 NT$80, 遠雄 NT$180, 統一 NT$200 — all present, all 100%, all nearer). The expected-cost ranker demotes them: 松壽廣場 = 20 walk + 70 money = 90 vs 君悅 = 15 + 160 = 175. Lever is EXPECTED_HOURS/TIME_VALUE in rank.ts, not the UI.
  Checked the NT$10-60 ranges against raw payex in data/cold/meta/: genuine CAR rates (10元/時 overnight 22-09, 60元/時 weekend day), NOT motorcycle-rate contamination. Display is honest; the midpoint used for SCORING is the approximation.
  FOR PLAN 3c: 188 lots price by time of day and the ranker scores a single midpoint, so overnight-cheap lots are over-rewarded during the day. payex already carries the hour windows.
  FOR PLAN 4: the null-probability path has ZERO real-world coverage — 0 of 1,088 lots have an UNKNOWN cell at +15 min. Re-check after a machine-sleep gap reaches the artifacts.
Task 4: complete (commit 7ca031d) — ranked list UI, 41 tests, build 64 KB gzip. Controller verified live in a browser at 375px: both languages render, lot names stay Chinese in English mode, staleness line shows 'data from 7 min ago'.
  THE THESIS VISIBLE ON SCREEN: 府前廣場 is the CLOSEST lot (191m) and among the cheapest, ranked SECOND because it is only 21% likely to have a space. Every existing app would show it first.
  OPEN PRODUCT QUESTION for the user: at EXPECTED_HOURS=2 the price term dominates walking distance, so 君悅 (NT$80, 188m, 100%) is demoted below 松壽廣場 (NT$10-60, 278m). Both lists agree on every underlying number; the lever is rank.ts constants, not the UI.
  OPEN: the range midpoint used for SCORING is time-blind — a lot priced NT$10 overnight / NT$60 weekend-day scores at NT$35 whatever the arrival hour. Affects 188 lots. Display is honest (shows the range); ranking is noisy.

=== PLAN 3b FINAL REVIEW: 1 Critical, 3 Important. Blocking ones fixed. 53 tests. ===
  C1 THE STALENESS CORRECTION WAS NEVER APPLIED — the app's central claim was wrong. Grid horizons are measured from base_data_ts, not from now, so 'arriving in 15 min' read the forecast for base+15. With a 23-min-old artifact that is now MINUS 8 min: a forecast for the past. And artifacts were fetched ONCE, so the error grew unbounded on an open tab. The spec is explicit that this offset IS the model's job ('the nowcast is a staleness correction'). Fixed: horizon + ageMin, plus a 120s refetch that pauses while the tab is hidden.
  I2 an unanswered geolocation prompt stranded the user forever — the spec's timeout only starts AFTER the permission decision, so a dismissed prompt fires neither callback. The file's own comment claimed this was impossible. Fixed with a watchdog.
  I3 documentElement.lang was hardcoded en while detectLang defaults to zh, so the common case announced a Chinese page as English to screen readers. Fixed.
  Reviewer verified exhaustively and found CLEAN: formatPrice over all 1,088 shipped rows prints no digit absent from that lot's own lo/hi; 27 unknown rows print words and no digit; 255 -> null -> 'no data' holds end to end while the 52 genuine 0 cells print 0%; roster pairing; lot names untranslated in both languages; 360px with the longest name; strict mode real not nominal.
PARKED to 3c: .slice(0,20) undoes rank.ts's never-drop guarantee once UNKNOWN cells appear (0 today, but CLAUDE.md documents time-correlated gaps); BASE_URL collapses // in an absolute CDN base; jest-dom not in setupFiles; empty-list state has no message; non-numeric coordinates unvalidated.
CONTROLLER ERROR FIXED: CLAUDE.md still said a 17-byte header / 26,129 bytes — stale since Plan 2 added roster_id. Now 21 bytes / 26,133.
