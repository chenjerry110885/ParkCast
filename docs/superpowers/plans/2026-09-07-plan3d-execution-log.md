# SDD ledger — plan: tasks/todo.md (Plan 3d — Roster honesty, first paint, search, install)

CONTROLLER GROUNDING BEFORE THE PLAN WAS WRITTEN: Plan 3a parked "7 motorcycle/bus lots"; re-measuring found **14**, all `totalcar: 0`, and **8 of them publishing P(free) of 98-100% at a car park with no car spaces**. TPE1697 (14 motorcycle bays, 0 car) reports 25-31 free cars; TPE0876 a constant 18. Also measured: `totalcar` is only ever positive (1,699) or exactly 0 (56) across 1,755 lots -- no -9 anywhere, which is exactly why the -9 test is the one that matters. And `rankLots` has no distance cutoff, so a user in Kaohsiung is ranked Taipei car parks 291 km away with a confident probability.

Task 1: complete (commit 7b9c333) — roster fix, 258 tests. One field (`Lot.serves_cars`) computed from the RAW `totalcar` before `clean_count` and `capacity or None` each independently collapse 0 and -9 into None; one clause in `publish_artifacts`. Collection path byte-identical in the diff. Measured off the live files: 1,089 -> 1,075 lots, grid.bin 26,157 -> 25,821 B, priced 97.5% -> 97.8%. Collector rebuilt BETWEEN the 08:11:30 and 08:16:30 ticks, no tick lost. Corpus continuity independently confirmed in the collector's own log: `rows=1174` stored, `published 1075`.
  Five mutations, each caught by exactly one test — incl. `int(raw) > 0` (treating -9 as zero-car) and `capacity_car=capacity` (letting 0 reach `validate`).
  IMPLEMENTER CORRECTED THE CONTROLLER: the plan implied the 14 lots' junk history INFLATES the climatology prior. Measured, it does the opposite — 6 of the 14 report a constant 0 free cars, so on balance they DEPRESS it. 7,803 of 640,814 observations (1.22%), own base rate 0.536, global prior 0.8966 with them vs 0.9010 without: a 0.44 pp depression. Still deferred (excluding them means invalidating Plan 2b's per-Parquet counter cache), but now deferred with the number and the right sign.

Task 2: complete (commit f7e243f) — lazy map, 101 tests. Render-blocking path **344.68 KB -> 67.54 KB gzip (-80%)**: entry JS 332.89 -> 66.25, entry CSS 11.79 -> 1.29, with MapLibre's 267.63 KB and its 82.86 KB stylesheet moved to a `MapView-*` chunk `index.html` does not reference. CSS split out on its own; no bundler hack needed. `MapView` is now default-export ONLY, so a stray `import { MapView }` cannot silently merge the chunk back.
  ZERO LAYOUT SHIFT, MEASURED not asserted: `.map` 242 -> 242 px, the two elements below at identical `top`, `scrollHeight` 2236 -> 2236, while the child went `map-placeholder` -> `maplibregl-map`. Required temporarily delaying the import 4 s to observe at all; reverted before commit.
  `mapChunk.test.tsx` is deliberately ONE test alone in its own file: `React.lazy` caches, so the window in which the map is provably absent exists exactly once per module registry — folded into `app.test.tsx` it would pass or fail on test ORDER.
  IMPLEMENTER CORRECTED THE PLAN: `app.test.tsx` had no synchronous map assertions; `mapSource.test.tsx` did.
  NOTE for future browser verification: `tabs_select` does NOT un-hide the Browser pane — taking a SCREENSHOT is what forces real frames. Supersedes the 3c note's implied fix.

Task 3: complete (commit b1bc522) — search + coverage boundary, 131 tests. Pure `searchLots` over the roster already in memory: no geocoder, no key, no third-party origin, and the destination never leaves the phone. `SEARCH_LIMIT = 8`. Total order on three keys (name-match before district-only, then match position, then feed id) so results never depend on array position.
  CONTROLLER-MEASURED, LOAD-BEARING: the feed is inconsistent about 臺 vs 台 — **76 published names use 臺, 108 use 台, and no single name uses both**. Without folding, `台北車站` finds 1 of the 4 Taipei Main Station car parks and `台大` 1 of 4. With it, 4 of 4 both ways. Folded in the SEARCH KEY only; the displayed name stays byte-identical to the feed because it has to match the sign on the building. Verified live: `台北車站` and `臺北車站` return byte-identical lists.
  `市政府` matching nothing is the HONEST limitation, not a bug — the car parks by City Hall are named 松壽廣場 and 府前廣場. The UI says it searches car park names.
  `COVERAGE_RADIUS_M = 10_000`, read off the NEAREST lot rather than the best-ranked one (at 291 km the ranking is a lottery between a thousand equally hopeless candidates). `noLotsNearby` DELETED rather than left dead.
  IMPLEMENTER CORRECTED THE CONTROLLER twice: (1) the coverage distances are approximate — Banqiao station is 2.70 km not 3.57, Taoyuan 23.45 not 23.63; same side of the line, but not quotable as measured constants. (2) `noLotsNearby` was not unreachable — it rendered in the empty-roster test fixture; it was unreachable *in production only*.
  Five mutations each caught by the right test, incl. `onSelect` calling `setDestination` instead of `pickDestination` — the tell is a stuck "Locating…" button.

Task 4: complete (commit a4fa7b9) — PWA, 32 tests. Icons from `scripts/build-icons.py` (pure `zlib`+`struct`, 4x supersampled, PNGs AND the SVG from one geometry function so they cannot drift). Hand-written `web/public/sw.js`, no build coupling, no new dependency. `routeFor` is pure and the tests evaluate the SHIPPED `public/sw.js` with a stubbed worker global, so they cannot drift from what deploys.
  Routing: basemap / `.pmtiles` / **any request carrying a `Range` header** -> never intercepted; `artifacts/*` -> network-first, cache fallback; navigations -> network-first; else in-scope -> cache-first; non-GET, cross-origin, out-of-scope -> passthrough. `isCacheable` rejects 206 by STATUS, not by `ok`. No `skipWaiting`, no `clients.claim`, asserted against the source.
  IMPLEMENTER CORRECTED THE CONTROLLER FIVE TIMES, all substantive:
    1. The controller's icon prototype's maskable variant WAS NOT MASKABLE — a 10% transparent border that a launcher's crop renders as chipped corners. Now full-bleed with the glyph inside the safe zone; two tests guard it.
    2. "App shell: cache-first" is WRONG for `index.html` — the one file Vite does not hash, so "a cached URL can never be stale" does not hold for it. Cache-first would pin the app to the first visit's bundle names forever. Navigations are network-first.
    3. "Works offline after one visit" is NOT achievable without a precache manifest: the worker does not control the page on the first load. Measured: load 1 installs, load 2 is the first it can cache, offline works from then. Documented honestly rather than claimed.
    4. "A new worker takes over on the next full load" is FALSE. Measured in Edge 152: after changing `sw.js`, a same-tab reload left the old worker active and the new one waiting; handover happened only after the client was released.
    5. `npm run preview` served the build at `/`, not `/ParkCast/` — every asset 404'd into the SPA fallback and came back as `index.html` with a 200. Fixed with `isPreview` in `vite.config.ts`.
  ALSO FOUND: the Browser pane cannot register service workers at all (`unknown error when fetching the script`, on a file the same page fetches at 200/`text/javascript`). Verification was driven through headless Edge 152 over CDP with a throwaway Node script, no dependency.
  Verified: `Page.getAppManifest` -> `errors: []`, scope resolved to `/ParkCast/` (proving relative `./` works under the sub-path); cache after two loads held the shell + both artifacts and NO pmtiles; ranged pmtiles fetch `206`, `fromServiceWorker: false`; offline reload rendered the full app from cache with an honest age line on a blank basemap.

Controller task (commit 289d2ed) — `MIN_REFETCH_MS = 30_000` floors the `visibilitychange` refetch, which was unbounded: one request per flip between apps, at a project whose entire serving story is "it is only a CDN". A sixth of the feed's cadence, so the floor can never be why a driver sees an older grid than exists — the interval decides freshness, this decides how often impatience can. Mutation: removing the guard fails exactly the two new tests, and the pre-existing "refetches when a hidden tab is looked at again" had to be updated because it asserted the very behaviour being bounded.
  Also swept the stale "1,088" prose out of 5 web source files. It had drifted TWICE (already wrong at 1,089 before Task 1 made it 1,075), so the fix was to make the prose roster-relative rather than to write a third number that will go stale. Execution logs left alone: they record what was true when written.

=== PLAN 3d FIRST REVIEW ATTEMPT: DIED MID-RUN, AND LEFT A MUTATION LIVE IN THE TREE ===
  The reviewer terminated on an API content filter partway through mutation testing. It left an INDEX-ALIGNMENT MUTATION in `App.tsx` — reading the grid by array position instead of through `Lot.i`, i.e. the exact bug class it had been sent to hunt. Caught because the controller re-ran the suite after reverting and got 5 failures on a commit already seen passing at 165; the numbers did not add up. Also left `web/src/search.ts` mid-mutation (restored by the agent before it died) and a CRLF/index artifact that `git diff` reported as empty while `git status` reported modified.
  LESSON FOR DISPATCH: a review agent that mutates the working tree must be told, as a protocol and not a suggestion, to keep at most ONE mutation live, revert it in the same tool call as the test run, and paste `git status --short` plus `git diff HEAD --stat` into its report as proof before concluding anything. Every subsequent agent got that protocol and every one of them left the tree clean.

CONTROLLER READ-ONLY AUDIT (run while the replacement reviewer worked, so as not to disturb its test runs):
  Index alignment CLEAN — every grid read funnels through `probabilityForLot`, which resolves `lot.i` and bounds-checks it. `rankLots` still reports array positions but the caller passes `rows[i]`, the LOT not the index.
  Bilingual completeness is COMPILER-ENFORCED, not grep-enforced: `Strings` is an interface and both dictionaries are declared `const en: Strings` / `const zh: Strings`, so a missing key is a build error. Green typecheck is a stronger proof than any search.
  No Simplified characters. The controller's own checker flagged 里 in 公里 — a FALSE POSITIVE of the checker: 里 is a legitimate Traditional character; the split is 裡/裏 vs 里.
  Staleness boundary 112 live / 113 expired still explicitly tested. Search touches no network (the only `fetch` in either search file is the word `fetchLots` in a comment). No external host anywhere in `web/src` outside comments about URL joining.

=== PLAN 3d SERVICE-WORKER REVIEW: 2 Critical, 2 Important. Verdict: NOT MERGEABLE. ===
  Method: 27 mutations one at a time under a try/finally driver, plus LIVE headless Edge 152 over CDP (zero-dependency client on Node 22's built-in WebSocket). 21 of 27 mutations caught — the `routeFor` decision table is well tested; the gap was precisely the EFFECTFUL code around a well-tested pure core.
  C1 A CACHE-WRITE FAILURE TURNED A HEALTHY 200 INTO A FAILED REQUEST. `keep()` awaited `cache.put()` with no try/catch and both routing paths awaited it before returning, so a rejection reached `respondWith()` and the browser rendered a network error for a response already successfully fetched. Proved with `Storage.overrideQuotaForOrigin` at 1 byte, network healthy: control "OK status 200" vs "REJECTED TypeError: Failed to fetch". `caches.match()` was unguarded too, so private mode or blocked site data failed EVERY intercepted request. The worker was strictly worse than no worker, persistently.
  C2 THE CACHED SHELL COULD OUTLIVE ITS BUNDLES. `networkFirst` wrote navigations back, so `index.html` was cached the moment it arrived while its hashed bundles were cached separately and later. Losing signal in between — driving into a basement, this app's own headline scenario — left a shell referencing files not on disk. Proved with the server genuinely stopped: blank white page, correct title, `#root html len: 0`, unrecoverable while offline.
  I1 SIX MUTANTS SURVIVED ALL 165 TESTS, including the two the design rests on: nothing tested that the code which STORES responses ever calls `isCacheable` (so "we never cache a 206" had no test catching its removal), and deleting `cacheFirst`'s single cache-write line stopped offline working entirely with no test noticing. No test dispatched `install` or `activate` at all.
  I2 the cross-origin guard was untested and, under the documented `PARKCAST_BASE=/` root deploy, is the ONLY guard — the scope-prefix check is vacuous when `base.pathname` is `/`.
  REVIEWER SELF-CORRECTED, which is why the rest was trusted: its first two offline runs were invalid because `Network.emulateNetworkConditions` does NOT apply to fetches made from inside a service worker. It discarded them and killed the server instead.
  PROVEN SOUND under live testing: 206 handling is airtight (basemap, artifacts AND JS bundles all returned 206 with `fromSW=false`; `isCacheable(206)` is false while `206.ok` is true, so rejecting by STATUS not `ok` is load-bearing); the no-skipWaiting lifecycle behaves exactly as documented; `build-icons.py` reproduces the committed bytes to the SHA-256; the maskable icon clears the strict 80% safe CIRCLE (furthest glyph pixel 135.5 px vs 204.8 px radius), not merely the square inset its own test checks.
  CONTROLLER'S OWN README OVERSTATED IT: "keeps working with no signal" with no mention that a worker does not control the page that registers it. Fixed with the caveat stated plainly.

Fix pass: complete (commit e1cc4f8) — 165 -> 181 tests. `keep()` and a new `cached()` helper both swallow storage failures; `networkFirst` no longer writes navigations back, so the cached shell has exactly ONE writer, `install`; a bilingual static fallback inside `#root` (inline-styled, because the stylesheet may be the missing file; no colour set, so the app's light/dark themes still apply) turns a failed boot from a white page into a sentence. All six surviving mutants now CAUGHT, plus three new ones for the fixes themselves; the baseline column was re-measured by restoring the pre-change test file rather than taken on trust.
  IMPLEMENTER CORRECTED THE REVIEWER THREE TIMES:
    1. "artifact fetch REJECTED" holds only on a COLD cache. With a cached copy — the normal state after one online load — the old worker returned 200 and served the stale copy. The two symptoms the review listed are mutually exclusive on any given request; both were measured.
    2. The C2 fix is necessary but NOT SUFFICIENT, and "the offline pair stays shell_v1 + bundles_v1" overstates it. It closes the deploy-skew window. It cannot close the FIRST-VISIT window: a worker does not control the page that registers it, so load one caches the shell and no bundles. That residual is exactly what earns the `#root` fallback its place, and it is documented rather than claimed away.
    3. The sixth surviving mutant (removing `isCacheable`'s `type !== "opaque"` clause) is an EQUIVALENT MUTANT — an opaque response has status 0, so `status === 200` already rejects it, and the old test passing `status: 0` proved only what the status check guarantees. Unkillable because it changes no behaviour. The right response was to delete the dead code and substitute a real mutation of the same guarantee.
  RIG TRAPS, both learned by getting a plausible wrong answer first: reusing one CDP debugging port across runs silently reattaches to the PREVIOUS browser, which still has the other `sw.js` installed — it made both variants look identical. And `vite preview` answers a missing file with `index.html` and a 200, so any "delete an asset to break it" experiment lies in the convincing direction. The fix pass used a purpose-built static server that 404s honestly and can destroy a socket. Recorded as L006.

CONTROLLER CLEANUP: two orphaned servers left listening by subagents were stopped — a `vite preview` on 4173 and a scratchpad static server bound to `0.0.0.0`, i.e. serving the build to the whole local network. Worth checking for at the end of any plan that involved browser verification, under this project's no-new-attack-surface constraint.
