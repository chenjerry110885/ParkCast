# SDD ledger — plan: tasks/todo.md (Plan 3c — Map and Scrubber)
Task 1: complete (commit e5435b7) — basemap pipeline. Script refuses to download/execute anything; prints URL + SHA-256 + verify steps and exits non-zero. Archive 23.1 MB, gitignored, byte-identical to the controller's verified copy. Implementer independently re-verified the release facts against GitHub's API rather than trusting the brief.
Task 2: complete (commit 167e557) — map renders, 59 tests.
  TWO BUGS ONLY LOOKING COULD CATCH: (1) MapLibre 6 derives its worker URL from import.meta.url, which Vite points at .vite/deps in dev and dist/assets in build — the worker is in neither, so it 404s SILENTLY and nothing renders. Broke the production build too. Fixed with ?worker&url + setWorkerUrl. (2) lots appeared ~10s late because the hook published on 'load' (full tile frame) instead of 'style.load'.
  CARRIED TO TASK 3: pass the FULL ranked array to MapView, not ranked.slice(0, LIST_LIMIT) — the map must show all 1,088 lots even though the list is capped.
  PARKED: no street labels (glyphs would need a CDN fetch, defeating the self-hosting); dark-mode basemap chosen once at creation.
Task 3: complete (commit d68d98c) — tap-to-set-destination + scrubber, 62 tests. Browser-verified at 360px: basemap draws, 1,088 features on the map while the list shows 20, scrubbing +5->+120 gave 24 distinct colour fingerprints with ZERO network requests. Scrubber replaced the select (a native select is a modal wheel that covers the map).
  NOTE for future map verification: a HIDDEN browser pane sets document.hidden, rAF never fires, and MapLibre stalls mid-style-load with no error — the map looks blank and nothing is wrong. Cost two implementers real time.
Task 4: complete (commits 2c598f3, 3af9579) — 6 correctness gaps closed, 89 tests, mutation-verified.
  BEYOND THE BRIEF, load-bearing: rankLots scored lots by ARRAY POSITION while App.tsx read the grid at that position, so the new coordinate filter would have handed every later lot its NEIGHBOUR's forecast. The same index-misalignment roster_id prevents at file level, reproduced inside the client. Now resolved through the validated Lot.i.
  Item 1 chosen: adaptive list, not re-ranking unknowns among the rest — a cost missing its risk term is strictly smaller, so every unknown lot would outrank an identical known one, the same bias MEDIAN_PRICE_FALLBACK exists to prevent.
  Stale-forecast state verified live against a 400-min-old artifact: expired notice, scrubber disabled, all rows 無資料 with no percentage, names/distances/prices intact.
  BUNDLE: 64 KB -> 333 KB gzipped (MapLibre). Candidate for lazy-loading in 3d.

=== PLAN 3c FINAL REVIEW: 0 Critical, 2 Important, both fixed. Verdict: MERGEABLE. 93 tests. ===
  I1 the map showed ZERO of 1,088 lots until a destination was tapped — the branch's headline capability invisible on first paint, and the 'silently absent lot' rule violated at full scale. Nothing a dot needs comes from the destination. Fixed: the map projects from the loaded artifacts, resolving each grid row through Lot.i; Ranked now drives only list order and the pin.
  I2 forecastExpired tripped one window late: for ageMin 113-120 the slider was enabled and the heading claimed 'ranked for your arrival' while all 24 positions rendered the same clamped column. Now trips when even the NEAREST arrival clamps. Boundary 112 live / 113 expired.
  Reviewer proved on the BUILT BUNDLE, not just source: zero third-party hosts. No glyphs or sprites in the style (57 layers, all background/fill/line), every CSS url() a data: URI, every external hostname confined to error strings and <a href>. Controller confirmed in the running app: externalOrigins = [].
  Also proven: index alignment complete end to end (Ranked.index consumed nowhere outside rank.ts); no MapLibre/WebGL/listener leaks across language switch, destination change or unmount; basemap served by real 206 Partial Content range requests, 240 KB drawn from a 23 MB archive.
PARKED to 3d: MapView is a static import so the 333 KB gzip bundle blocks first paint — React.lazy restores a ~64 KB list-first render, the one parked item with user-visible cost. Also: no street labels (glyphs would need a CDN fetch); visibilitychange refetch has no minimum interval; noLotsNearby unreachable with a non-empty roster.
